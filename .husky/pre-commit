# #!/usr/bin/env sh
# . "$(dirname -- "$0")/_/husky.sh"

# # Get staged files with extensions prettier handles, excluding deleted files
# STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(js|jsx|ts|tsx|json|css|scss|md|html)$')

# if [ -n "$STAGED_FILES" ]; then
#   echo "Running Prettier on staged files..."
#   echo "$STAGED_FILES" | xargs npx prettier --write

#   # Re-add the formatted files to staging
#   echo "$STAGED_FILES" | xargs git add
# fi
npx lint-staged